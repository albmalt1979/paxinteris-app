Expo frontend:
- Install expo-cli (npm install -g expo-cli)
- cd frontend
- npm install
- npm start

Note: in Android emulator use 10.0.2.2 to reach host machine (backend). Adjust base URLs if running on device.
